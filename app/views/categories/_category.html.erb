<tr>
  <td><%= link_to category.name, category_lessons_path(category.id) %></td>
  <td><%= category.duration %></td>
  <td><%= category.word_per_lesson %></td>
  <td><%= "#{@words_count[category.id]} / #{category.words.size}" %></td>
</tr>
