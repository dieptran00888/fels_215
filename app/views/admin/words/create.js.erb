<% if @word.errors.any? %>
  $("#div-error-messages").html(
    "<%= j render("shared/error_messages", object: @word) %>");
<% else %>
  $('#div-words').prepend("<%= j render @word %>");
  $('input[type=text]').val("");
  $('input[type=checkbox]').attr('checked', false);
<% end %>
