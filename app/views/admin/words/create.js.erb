<% if @word.errors.any? %>
  alert("Something wrong with input value! Try again!");
<% else %>
  $('#div-words').prepend("<%= j render @word %>");
  $('input[type=text]').val("");
  $('input[type=checkbox]').attr('checked', false);
<% end %>
